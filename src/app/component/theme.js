import { createTheme } from "@mui/material/styles";

// Use the same font as from next/font/google
const theme = createTheme({
  typography: {
    fontFamily: "Roboto",
  },
});

export default theme;
